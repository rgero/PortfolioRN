import { Surface, Text } from "react-native-paper";

const ResumeScreen = () => {
  return (
    <Surface style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text variant="displayLarge">Resume</Text>
    </Surface>
  );
}

export default ResumeScreen
